import 'package:flutter/material.dart';

class ColorConstants {
  // static const Color primaryColor = Color.fromARGB(a, r, g, b);
  static const Color accentColor = Color.fromARGB(148, 255, 155, 4);
  static const Color btnEnabled = Color.fromARGB(187, 255, 154, 4);
  static const Color btnOnPressed = Color.fromARGB(147, 194, 167, 128);
  // Add more color constants as needed for your project
}
